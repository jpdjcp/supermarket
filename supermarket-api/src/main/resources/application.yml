spring:
  application:
    name: supermarket-api

  jpa:
    open-in-view: true

  profiles:
    active: ${SPRING_PROFILES_ACTIVE}

security:
  jwt:
    secret: ${JWT_SECRET}

server:
  port: ${SERVER_PORT:8080}