name: Docs check

on:
  push:
    paths:
      - 'README.md'
      - 'docs/**'

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Docs updated
        run: echo "Documentation change detected"
