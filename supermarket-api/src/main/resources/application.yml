spring:
  application:
    name: supermarket-api

  jpa:
    open-in-view: false

  profiles:
    active: ${SPRING_PROFILES_ACTIVE}

security:
  jwt:
    secret: ${JWT_SECRET}

server:
  port: ${SERVER_PORT:8080}